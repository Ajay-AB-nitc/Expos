int main() {
    decl
        int temp;
        str fileName;
    enddecl

    begin
        write("file to exec");
        temp = exposcall("Read", -1, fileName);
        temp = exposcall("Exec", fileName);
        if (temp == -1) then
            write("No file found");
        endif;
        return 0;
    end
}
