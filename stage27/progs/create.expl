int main() {
    decl
       str name;
       int temp, permission; 
    enddecl
    begin
        write("File name: ");
        read(name);
        write("Permission: ");
        read(permission);
        temp = exposcall("Create", name, permission);
        breakpoint;
        write(temp);
        return 0;
    end
}